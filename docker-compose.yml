services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}"
    environment:
      PORT: ${PORT}
      BASE_URL: ${BASE_URL}
      DATABASE_PATH: /data/payments.db
    volumes:
      - payments-data:/data
    restart: unless-stopped

volumes:
  payments-data:
